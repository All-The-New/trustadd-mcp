{
  "buildCommand": "npm run build:client",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/sitemap-agents.xml", "destination": "/api/sitemap-agents.xml" },
    { "source": "/:path((?!api/).*)", "destination": "/index.html" }
  ]
}
